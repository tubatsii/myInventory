
  # PANDA Imventory Management System

This is a code bundle for PANDA Imventory Management System. 

  ## Running the code

  Run `npm i` or `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.
  
